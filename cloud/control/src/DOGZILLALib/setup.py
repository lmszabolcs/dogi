from setuptools import find_packages, setup

setup(
    name='DOGZILLALib',
    version='3.1.9',
    author='Yahboom Team',
    packages=find_packages(),
)

# cd py_install
# sudo python3 setup.py install
